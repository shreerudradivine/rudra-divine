<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <!-- 
         - primary meta tags
         -->
      <title>Vegga</title>
      <meta name="title" content="Vegga">
      <meta name="description" content=".">
      <!-- 
         - favicon
         -->
      <link rel="shortcut icon" href="favicon.svg" type="image/svg+xml">
      <!-- 
         - custom css link
         -->
      <link rel="stylesheet" href="/main.css">
      <!-- 
         - google font link
         -->
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link
         href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;500;600;700&family=Poppins:wght@400;500;600;700;800&display=swap"
         rel="stylesheet">
      <!-- 
         - preload images
         -->
      <link rel="preload" as="image" href="hero-banner.png">
   </head>
   <body>
<%- include('parts/nav') %>
      <main>
         <article>
            <!-- 
               - #add
               -->
            <section class="section market" aria-label="crypto market live price">
               <div class="container">
                  <h1 class="h2 section-title text-center">Update a input</h1>
                  <p class="section-text text-center">
Serial Number : <%= item.serialNumber %>
                  </p>
                  <div class="con">
                     <form class="form" action="/admin/edit/<%= item.serialNumber %>" method="POST">
                        <div class="input-container">
                           <input placeholder="Name :<%= item.name %>" type="text" id="name" name="name" required>
                        </div>
                        <div class="input-container">
                           <input placeholder="Description :<%= item.description %>" type="text" id="description" name="description" required>
                        </div>
                        <button class="submit" type="submit">
                        Update 
                        </button>
                     </form>
                  </div>
               </div>
            </section>

         </article>
      </main>
     
<%- include('parts/footer2') %>
  
      <!-- 
         - custom js link
         -->
      <script src="/script.js" defer></script>
      <!-- 
         - ionicon-link
         -->
      <script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
      <script nomodule src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.js"></script>
   </body>
</html>